# spec 046 Q77/Q82 — rebuildable caches, per-machine
market-data/
raw-data/
# evidence quote snapshots inside each thesis ARE committed


*.zip

# macOS
.DS_Store
# report renders — regenerable via scripts/render_report.py (Chrome+poppler)
theses/*/report/pages/
